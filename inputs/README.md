# Required input

Place the formally certified response-curve parameterization here as:

```text
global_parameterization.json
```

Its canonical JSON SHA-256 must be:

```text
e8ad8a6fbcab626b726082b570f59df6854d4a28259177783e1f5e3274b1cb84
```

The input originates from the companion Projective-Jet response-fibre
certificate.  It is not bundled in this migration ZIP because the exact
generated artifact was not available in the repository audit workspace.

