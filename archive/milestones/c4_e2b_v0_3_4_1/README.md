# C4-E2b transition 1→2 recovery bundle v0.3.4.1

This directory freezes four historical research drivers. They are post-publication
continuation work and do not enlarge the v1.2.12 manuscript or v0.7.4 + v0.9.3
published theorem boundary.

The frozen scientific state before this bundle was prepared is:

- transition 0→1: certified prerequisite passed;
- transition 1→2 at 32 slabs: inconclusive, not failed and not certified;
- endpoint centre overlap margin: `9.923e-08`;
- complete endpoint radius: `2.320e-07`;
- complete-box overlap margin: `-1.320e-07`;
- next step: local recenter/affine handoff followed by the controller-covariance diagnostic.

The parent driver pins upstream repository commit
`f391925b3219070f8f843f2043e0b265ea2eb3b9` and fetches the required modules and
E2a certificate from that immutable source boundary.

Use `python reproduce/c4_e2b_resume.py ...` rather than moving or renaming these
historical files.
